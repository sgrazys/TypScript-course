{
	"name": "practice-project",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "dead-server --port=1410 --host=localhost"
	},
	"author": "Saulius Grazys",
	"license": "MIT",
	"devDependencies": {
		"dead-server": "^1.0.9"
	}
}
